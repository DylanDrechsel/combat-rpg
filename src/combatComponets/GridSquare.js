import React from 'react';

const GridSquare = (props) => {
    // const classes = `grid-square color-${props.color}`;

    return (
		<div style={{height: "120px", width: "120px", border: "5px solid"}} />
		);
};

export default GridSquare;